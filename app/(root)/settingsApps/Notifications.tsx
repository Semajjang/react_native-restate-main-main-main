import { SafeAreaView, Text } from "react-native";

const Notifications = () => {
    return (
        <SafeAreaView className="flex-1 items-center justify-center bg-white">
            <Text className="text-xl font-bold">Notifications Page</Text>
        </SafeAreaView>
    );
};

export default Notifications;
