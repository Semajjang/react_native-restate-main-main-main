import { SafeAreaView, Text } from "react-native";

const Security = () => {
    return (
        <SafeAreaView className="flex-1 items-center justify-center bg-white">
            <Text className="text-xl font-bold">Security</Text>
        </SafeAreaView>
    );
};

export default Security;
